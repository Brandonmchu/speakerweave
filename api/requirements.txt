fastapi
uvicorn[standard]
python-dotenv
pydantic[email]
supabase==2.27.1
PyJWT
# HTML sanitizer for organizer-authored rich text (welcome_html /
# confirmation_html) rendered on the public form via dangerouslySetInnerHTML.
bleach
httpx
# AI first-pass triage over submitted abstracts (services/ai_triage.py). The
# module degrades to a score-based heuristic when ANTHROPIC_API_KEY is unset,
# so a deployment without a key still answers.
anthropic
python-multipart
slowapi
anyio
# Official Model Context Protocol SDK for the hosted /mcp endpoint.
mcp==2.0.0
# zoneinfo needs a tz database; slim container images often ship without one,
# and invite emails render local times (services/ics.py falls back to UTC).
tzdata

# Testing / lint
pytest
pytest-asyncio
pytest-timeout
ruff
